#!/bin/awk -f

# if not running using awk -f awk_script then shebang for awk is must

# not that begin in awk can be used without any input while end and main action block requires inputs

BEGIN{print "Hello world"}

