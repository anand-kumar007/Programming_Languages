//2) Which one of the following is considered as the least safe typecasting in C++?
// (A) static_cast
// (B) const_cast
// (C) reinterpret_cast  (correct)
// (D) dynamic_cast

    //Description: Typecasting is referred to as converting a specific expression type to another type. However, in the C++ programming language,

    // the reinterpret_ cast is considered as the least safe typecasting.

// Q. dynamic_cast can be used only with pointers and references to objects
//  Answer : yes

// Q. dynamic_cast is always successful when we cast a class to one of its base classes.

// Answer : yes

// Q. reinterpret_cast converts any pointer type to any other pointer type, even of unrelated classes.

// True